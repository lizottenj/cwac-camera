This is a custom build of CWAC-Camera-v9 that removes the ActionBarSherlock
dependency. Once the new version of CWAC-Camera is release we should be able
to remove this and replace it with the official build